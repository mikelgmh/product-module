<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="product-module.list">
      <field name="name">product-module list</field>
      <field name="model">product-module.product-module</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="product-module.action_window">
      <field name="name">product-module window</field>
      <field name="res_model">product-module.product-module</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="product-module.action_server">
      <field name="name">product-module server</field>
      <field name="model_id" ref="model_product-module_product-module"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="product-module" id="product-module.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="product-module.menu_1" parent="product-module.menu_root"/>
    <menuitem name="Menu 2" id="product-module.menu_2" parent="product-module.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="product-module.menu_1_list" parent="product-module.menu_1"
              action="product-module.action_window"/>
    <menuitem name="Server to list" id="product-module" parent="product-module.menu_2"
              action="product-module.action_server"/>
    -->
  </data>
</odoo>